{
  "name": "mcp-capability-runtime",
  "version": "0.3.0",
  "description": "A MCP-compatible capability runtime demo that turns flat tool lists into capability surfaces.",
  "type": "module",
  "license": "MIT",
  "private": false,
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "import": "./dist/src/index.js"
    }
  },
  "bin": {
    "mcp-capability-runtime": "./dist/src/cli.js"
  },
  "files": [
    "dist/src",
    "README.md",
    "docs",
    "examples",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test \"dist/tests/**/*.test.js\"",
    "lint": "tsc -p tsconfig.json --noEmit",
    "demo:tax": "npm run build && node dist/src/cli.js tax --demo",
    "example:tax": "npm run build && node dist/src/cli.js tax --input examples/minimal-tool-surface.json",
    "raw:tax": "npm run build && node dist/src/cli.js tax --input examples/raw-tool-surface.json",
    "prepack": "npm run build",
    "pack:check": "npm --cache ./.npm-cache pack --dry-run --json"
  },
  "keywords": [
    "mcp",
    "capabilities",
    "agents",
    "tooling",
    "runtime"
  ],
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "22.19.21",
    "typescript": "6.0.3"
  }
}
